# Reports Directory

A genuine `report_output.md` must be generated through the Gemini path with valid `GEMINI_API_KEY` access. The repository does not fabricate or include a StubProvider-generated submission report. The `--skip-llm` mode is reserved for offline testing and produces a draft artifact only.
